aiohttp>=3.8
numpy>=1.24
pillow>=9.0
streamlit>=1.10
tensorflow>=2.11
h5py>=3.1

# Notes:
# - `tensorflow` may be heavy; install `tensorflow-cpu` or GPU builds as needed.
# - Adjust versions if you have existing environment constraints.
